version: "3.9"

services:
  api:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data          # SQLite 持久化
    environment:
      - DATABASE_URL=sqlite:///./data/items.db
      - JWT_SECRET_KEY=change-me-in-production
